# gateway.port=8090
# gateway.identity-path=/api/auth/**, /api/users/**, /v3/api-docs/**, /swagger-ui/**
# gateway.project-path=/api/projects/**,/api/tasks/**,/api/statistiques/**, /v3/api-docs/**, /swagger-ui/**
# gateway.urlregistry=http://localhost:8761/eureka/

# gateway.identity-path=/api/auth/**, /api/users/**, /identity-service/v3/api-docs
# gateway.project-path=/api/projects/**, /api/tasks/**, /api/statistiques/**, /project-service/v3/api-docs

# # 1. On active le support de base
# springdoc.enable-native-support=true
# springdoc.swagger-ui.path=/swagger-ui.html

# # 2. On désactive le locator automatique qui pose problème
# springdoc.gateway.discovery.locator.enabled=false

# # 3. ON FORCE LE MENU DÉROULANT EN DUR (Sans toucher aux microservices !)
# springdoc.swagger-ui.urls[0].name=Identity Service
# springdoc.swagger-ui.urls[0].url=/identity-service/v3/api-docs

# springdoc.swagger-ui.urls[1].name=Project Service
# springdoc.swagger-ui.urls[1].url=/project-service/v3/api-docs
gateway.port=8090
gateway.urlregistry=http://localhost:8761/eureka/

# 📂 1. Tes chemins de base (propres)
gateway.identity-path=/api/auth/**, /api/users/**, /identity-service/v3/api-docs
gateway.project-path=/api/projects/**, /api/tasks/**, /api/statistiques/**, /project-service/v3/api-docs

# 🤖 2. Route Identity-Service avec nettoyage des en-têtes bloquants
spring.cloud.gateway.routes[0].id=identity-service
spring.cloud.gateway.routes[0].uri=lb://identity-service
spring.cloud.gateway.routes[0].predicates[0]=Path=${gateway.identity-path}
# 💡 Supprime l'en-tête d'authentification pour les appels de doc pour éviter le 403
spring.cloud.gateway.routes[0].filters[0]=RemoveRequestHeader=Authorization

# 🤖 3. Route Project-Service avec nettoyage des en-têtes bloquants
spring.cloud.gateway.routes[1].id=project-service
spring.cloud.gateway.routes[1].uri=lb://project-service
spring.cloud.gateway.routes[1].predicates[0]=Path=${gateway.project-path}
# 💡 Identique ici, on nettoie la requête sortante
spring.cloud.gateway.routes[1].filters[0]=RemoveRequestHeader=Authorization


# 🚀 4. Configuration de l'affichage Swagger
springdoc.enable-native-support=true
springdoc.swagger-ui.path=/swagger-ui.html

springdoc.swagger-ui.urls[0].name=Identity Service
springdoc.swagger-ui.urls[0].url=/identity-service/v3/api-docs

springdoc.swagger-ui.urls[1].name=Project Service
springdoc.swagger-ui.urls[1].url=/project-service/v3/api-docs